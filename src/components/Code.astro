---
const { title } = Astro.props;
---

<div class="rounded-lg border bg-card text-card-foreground shadow-sm w-full max-w-7xl mx-auto">
    <div class="flex flex-col space-y-1.5 p-6 pb-2"><h3
            class="tracking-tight inline-block px-2 py-1 text-sm font-bold bg-primary/10 rounded">{title}</h3></div>
    <div class="p-6 pt-0">
        <pre class="max-h-[400px] overflow-auto rounded-lg bg-muted p-4 shadow-sm bg-white font-mono text-xs"><slot /></pre>
    </div>
</div>